import { Typography } from 'antd'

const { Title, Paragraph } = Typography

export function CategoriesPage() {
  return (
    <div>
      <Title level={3}>Danh mục</Title>
      <Paragraph>Master data — danh mục sản phẩm / nhóm.</Paragraph>
    </div>
  )
}

import { Typography } from 'antd'

const { Title, Paragraph } = Typography

export function AttributesPage() {
  return (
    <div>
      <Title level={3}>Thuộc tính</Title>
      <Paragraph>Master data — thuộc tính biến thể.</Paragraph>
    </div>
  )
}

import { Typography } from 'antd'

const { Title, Paragraph } = Typography

export function AttributeValuesPage() {
  return (
    <div>
      <Title level={3}>Giá trị thuộc tính</Title>
      <Paragraph>Master data — giá trị cho từng thuộc tính.</Paragraph>
    </div>
  )
}

import { Typography } from 'antd'

const { Title, Paragraph } = Typography

export function ProductVariantsPage() {
  return (
    <div>
      <Title level={3}>Biến thể / SKU</Title>
      <Paragraph>Danh sách biến thể sản phẩm (SKU).</Paragraph>
    </div>
  )
}

